<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_splash_screen"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/colorPrimary"
    tools:context="mn.signlanguage.SplashScreen">

    <TextView
        android:text="Дохионы хэл"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/splashText"
        android:textSize="36sp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="180dp" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/splashImageView"
        android:src="@mipmap/ic_launcher"
        android:layout_alignLeft="@+id/splashText"
        android:layout_alignStart="@+id/splashText"
        android:layout_alignRight="@+id/splashText"
        android:layout_alignEnd="@+id/splashText"
        android:layout_above="@+id/splashText"
        android:layout_alignParentTop="true" />
</RelativeLayout>
